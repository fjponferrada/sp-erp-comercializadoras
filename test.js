const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const lead = await prisma.lead.findFirst({ where: { businessName: { contains: 'Ponferrada' } }, orderBy: { updatedAt: 'desc' } }); console.log(JSON.stringify({ address: lead.address, cData: lead.contractData, lastName: lead.lastName }, null, 2)); } main().finally(() => prisma.$disconnect());
